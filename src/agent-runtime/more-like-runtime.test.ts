import { describe,expect,it,vi } from 'vitest'
import { executeMoreLike } from './more-like-runtime'
import type { GlobalLibraryItem } from '@/global-library'
const at='2026-07-12T00:00:00.000Z',hash='a'.repeat(64)
const parent={protocol:'cutout.global-library.v1',id:'asset.parent',version:'1.0.0',kind:'visual-asset',name:'Parent',description:'Approved parent',contentSha256:hash,content:{manifestPath:'manifest.json',manifestSha256:hash,artifacts:[{path:'parent.png',sha256:hash,mediaType:'image/png',size:1}]},origin:{kind:'bundled',producer:'cutout'},license:{kind:'proprietary',holder:'Owner',usage:'Project use'},tags:[],collections:[],favorite:false,pinned:false,dependencies:[],compatibility:[],qualityReceipts:[],lineage:{root:{itemId:'asset.parent',version:'1.0.0',contentSha256:hash},depth:0},createdAt:at,updatedAt:at} as GlobalLibraryItem
describe('more-like runtime',()=>{it('requires real generated bytes and writes exact parent lineage',async()=>{const forkItem=vi.fn(),next=await executeMoreLike({request:{requestId:'req',instruction:'same composition, cooler light',parent:{id:parent.id,version:parent.version,contentSha256:parent.contentSha256}},parent,executor:{execute:async()=>({receipt:{receiptId:'receipt',requestId:'req',capability:'generate-image',providerId:'provider',model:'model',status:'succeeded',charged:{currency:'USD',amount:1},outputArtifactIds:['artifact'],startedAt:1,completedAt:2},artifacts:[{path:'variant.png',mediaType:'image/png',bytes:new Uint8Array([1,2])}]})},blobs:{put:async()=>({sha256:'b'.repeat(64),size:2,mediaType:'image/png'})} as never,library:{forkItem} as never,now:at});expect(next.lineage.parent).toEqual({itemId:parent.id,version:parent.version,contentSha256:parent.contentSha256});expect(forkItem).toHaveBeenCalledOnce()})})
