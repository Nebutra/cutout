import{invoke as tauriInvoke}from'@tauri-apps/api/core';import type{DurableHostEvent}from'./run-event-projection'
export interface TauriHostSnapshot{readonly status:string;readonly events:readonly DurableHostEvent[];readonly runs:Readonly<Record<string,unknown>>}
type Invoke=(command:string,args?:Record<string,unknown>)=>Promise<unknown>
export function createTauriAgentHostService(input:{workspaceHandle:string;instanceId:string;invoke?:Invoke;window?:Pick<Window,'addEventListener'|'removeEventListener'>}){const call=input.invoke??tauriInvoke,base={workspaceHandle:input.workspaceHandle},start=()=>call('agent_host_start',{...base,instanceId:input.instanceId})as Promise<TauriHostSnapshot>,status=()=>call('agent_host_status',base)as Promise<TauriHostSnapshot>,recover=()=>call('agent_host_recover',{...base,instanceId:input.instanceId})as Promise<TauriHostSnapshot>,shutdown=()=>call('agent_host_shutdown',base)as Promise<TauriHostSnapshot>;return{start,status,recover,shutdown,installLifecycle(){const target=input.window??globalThis.window;let disposed=false;const wake=()=>{void recover()};const stop=()=>{void shutdown()};target?.addEventListener('pageshow',wake);target?.addEventListener('beforeunload',stop);const started=start();return{started,dispose(){if(disposed)return;disposed=true;target?.removeEventListener('pageshow',wake);target?.removeEventListener('beforeunload',stop);void shutdown()}}}}}

export function bindTauriAgentHostLifecycle(input:{getWorkspace:()=>{readonly handle:string}|undefined;subscribe:(listener:(workspace:{readonly handle:string}|undefined)=>void)=>(()=>void);create:(workspaceHandle:string)=>ReturnType<typeof createTauriAgentHostService>;onSnapshot?:(snapshot:TauriHostSnapshot)=>void}){let active:ReturnType<ReturnType<typeof createTauriAgentHostService>['installLifecycle']>|undefined,currentHandle:string|undefined,closed=false;const select=(workspace:{readonly handle:string}|undefined)=>{if(closed||workspace?.handle===currentHandle)return;active?.dispose();active=undefined;currentHandle=workspace?.handle;if(!workspace)return;const lifecycle=input.create(workspace.handle).installLifecycle();active=lifecycle;void lifecycle.started.then(snapshot=>{if(!closed&&currentHandle===workspace.handle)input.onSnapshot?.(snapshot)})};select(input.getWorkspace());const unsubscribe=input.subscribe(select);return()=>{closed=true;unsubscribe();active?.dispose();active=undefined;currentHandle=undefined}}
