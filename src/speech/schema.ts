import{z}from'zod';export const speechPreferencesSchema=z.object({version:z.literal('cutout.speech-preferences.v1'),microphoneDeviceId:z.string().max(240),activationMode:z.enum(['push-to-talk','toggle']),shortcut:z.string().max(80),keepDictationVisible:z.boolean(),dictionary:z.array(z.string().trim().min(1).max(120)).max(200),ttsVoice:z.string().max(160),ttsRate:z.number().min(.5).max(2),ttsAutoPlay:z.boolean()}).strict();export type SpeechPreferences=z.infer<typeof speechPreferencesSchema>;export const defaultSpeechPreferences:SpeechPreferences={version:'cutout.speech-preferences.v1',microphoneDeviceId:'',activationMode:'push-to-talk',shortcut:'',keepDictationVisible:true,dictionary:[],ttsVoice:'',ttsRate:1,ttsAutoPlay:false}
