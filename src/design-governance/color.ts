export interface Color{readonly r:number;readonly g:number;readonly b:number;readonly a:number;readonly source:'srgb'|'display-p3'|'oklch'}
export function parseCssColor(input:string):Color{const value=input.trim().toLowerCase();if(/^#[0-9a-f]{3,8}$/.test(value))return hex(value);const rgb=value.match(/^rgba?\(\s*([\d.]+)%?[, ]+([\d.]+)%?[, ]+([\d.]+)%?(?:\s*[/,]\s*([\d.]+)%?)?\s*\)$/);if(rgb){const percent=value.includes('%');return color(clamp(+rgb[1]!/(percent?100:255)),clamp(+rgb[2]!/(percent?100:255)),clamp(+rgb[3]!/(percent?100:255)),alpha(rgb[4],value),'srgb')}const p3=value.match(/^color\(display-p3\s+([\d.]+)\s+([\d.]+)\s+([\d.]+)(?:\s*\/\s*([\d.]+)%?)?\)$/);if(p3){const [x,y,z]=p3ToSrgb(+p3[1]!,+p3[2]!,+p3[3]!);return color(x,y,z,alpha(p3[4],value),'display-p3')}const ok=value.match(/^oklch\(\s*([\d.]+)%?\s+([\d.]+)\s+(-?[\d.]+)(deg|grad|rad|turn)?(?:\s*\/\s*([\d.]+)%?)?\s*\)$/);if(ok){const l=+ok[1]!/(/^oklch\(\s*[\d.]+%/.test(value)?100:1),c=+ok[2]!,h=angleRadians(+ok[3]!,ok[4]);const [r,g,b]=oklabToSrgb(l,c*Math.cos(h),c*Math.sin(h));return color(r,g,b,alpha(ok[5],value),'oklch')}throw new Error(`Unsupported CSS Color 4 value: ${input}`)}
export function normalizeSrgb(value:Color){const mapped=inGamut(value)?value:gamutMap(value);return `rgb(${round(mapped.r*255)} ${round(mapped.g*255)} ${round(mapped.b*255)}${mapped.a<1?` / ${round(mapped.a,3)}`:''})`}
export function composite(foreground:Color,background:Color):Color{const a=foreground.a+background.a*(1-foreground.a);if(a===0)return color(0,0,0,0,'srgb');return color((foreground.r*foreground.a+background.r*background.a*(1-foreground.a))/a,(foreground.g*foreground.a+background.g*background.a*(1-foreground.a))/a,(foreground.b*foreground.a+background.b*background.a*(1-foreground.a))/a,a,'srgb')}
export function contrastRatio(a:Color,b:Color){const x=opaque(a,b),y=opaque(b,color(1,1,1,1,'srgb'));const l1=luminance(x),l2=luminance(y);return(Math.max(l1,l2)+.05)/(Math.min(l1,l2)+.05)}
export function deltaE(a:Color,b:Color){const x=srgbToOklab(a),y=srgbToOklab(b);return Math.hypot(x[0]-y[0],x[1]-y[1],x[2]-y[2])*100}
export function simulateColorVision(c:Color,kind:'protanopia'|'deuteranopia'|'tritanopia'):Color{const m=kind==='protanopia'?[[.152,.853,-.005],[.115,.786,.099],[-.004,-.048,1.052]]:kind==='deuteranopia'?[[.367,.861,-.228],[.28,.673,.047],[-.012,.043,.969]]:[[1.256,-.077,-.179],[-.078,.931,.148],[.005,.691,.304]];return color(...(m.map((row)=>clamp(row[0]!*c.r+row[1]!*c.g+row[2]!*c.b))as[number,number,number]),c.a,'srgb')}
function hex(v:string){let s=v.slice(1);if(s.length===3||s.length===4)s=[...s].map((x)=>x+x).join('');return color(parseInt(s.slice(0,2),16)/255,parseInt(s.slice(2,4),16)/255,parseInt(s.slice(4,6),16)/255,s.length===8?parseInt(s.slice(6,8),16)/255:1,'srgb')}
function alpha(v:string|undefined,source:string){if(!v)return 1;return clamp(+v/(source.match(/\/\s*[\d.]+%/) ?100:1))}function color(r:number,g:number,b:number,a:number,source:Color['source']):Color{return{r,g,b,a,source}}function clamp(v:number){return Math.min(1,Math.max(0,v))}function round(v:number,n=1){const p=10**n;return Math.round(v*p)/p}function lin(v:number){return v<=.04045?v/12.92:((v+.055)/1.055)**2.4}function gam(v:number){return v<=.0031308?12.92*v:1.055*Math.sign(v)*Math.abs(v)**(1/2.4)-.055}function luminance(c:Color){return.2126*lin(c.r)+.7152*lin(c.g)+.0722*lin(c.b)}function opaque(a:Color,b:Color){return a.a<1?composite(a,b):a}function inGamut(c:Color){return[c.r,c.g,c.b].every((v)=>v>=0&&v<=1)}
function p3ToSrgb(r:number,g:number,b:number){const lr=lin(r),lg=lin(g),lb=lin(b),x=.4865709*lr+.2656677*lg+.1982173*lb,y=.2289746*lr+.6917385*lg+.0792869*lb,z=.0451134*lg+1.0439444*lb;return[gam(3.2406*x-1.5372*y-.4986*z),gam(-.9689*x+1.8758*y+.0415*z),gam(.0557*x-.204*y+1.057*z)]as const}
function angleRadians(value:number,unit:string|undefined){if(unit==='rad')return value;if(unit==='turn')return value*Math.PI*2;if(unit==='grad')return value*Math.PI/200;return value*Math.PI/180}
function oklabToSrgb(L:number,a:number,b:number){const l=(L+.3963377774*a+.2158037573*b)**3,m=(L-.1055613458*a-.0638541728*b)**3,s=(L-.0894841775*a-1.291485548*b)**3;return[gam(4.0767416621*l-3.3077115913*m+.2309699292*s),gam(-1.2684380046*l+2.6097574011*m-.3413193965*s),gam(-.0041960863*l-.7034186147*m+1.707614701*s)]as const}
function srgbToOklab(c:Color){const r=lin(c.r),g=lin(c.g),b=lin(c.b),l=Math.cbrt(.4122214708*r+.5363325363*g+.0514459929*b),m=Math.cbrt(.2119034982*r+.6806995451*g+.1073969566*b),s=Math.cbrt(.0883024619*r+.2817188376*g+.6299787005*b);return[.2104542553*l+.793617785*m-.0040720468*s,1.9779984951*l-2.428592205*m+.4505937099*s,.0259040371*l+.7827717662*m-.808675766*s]as const}
function gamutMap(c:Color){const [L,a,b]=srgbToOklab(c),h=Math.atan2(b,a),chroma=Math.hypot(a,b);let low=0,high=chroma,result=color(clamp(c.r),clamp(c.g),clamp(c.b),c.a,c.source);for(let i=0;i<24;i++){const mid=(low+high)/2,[r,g,bb]=oklabToSrgb(L,mid*Math.cos(h),mid*Math.sin(h)),candidate=color(r,g,bb,c.a,c.source);if(inGamut(candidate)){result=candidate;low=mid}else high=mid}return result}
