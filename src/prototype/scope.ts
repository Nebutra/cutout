export type PrototypeSuiteScope = 'primary-flow' | 'full-plan'

/** New projects generate every route selected by the Planner Agent. */
export const DEFAULT_PROTOTYPE_SUITE_SCOPE: PrototypeSuiteScope = 'full-plan'
