import{afterEach,describe,expect,it,vi}from'vitest';import{clearAuthorizedWorkspaceForProjectTransition,getAuthorizedWorkspace,setAuthorizedWorkspace,subscribeAuthorizedWorkspace}from'./authorized-workspace'
afterEach(()=>setAuthorizedWorkspace(undefined))
describe('authorized workspace session',()=>{it('shares only an opaque authorized handle and never a caller path',()=>{const listener=vi.fn(),dispose=subscribeAuthorizedWorkspace(listener);setAuthorizedWorkspace({handle:'workspace.opaque-1',label:'Authorized workspace'});expect(getAuthorizedWorkspace()).toEqual({handle:'workspace.opaque-1',label:'Authorized workspace'});expect(listener).toHaveBeenCalledOnce();expect(()=>setAuthorizedWorkspace({handle:'/Users/alice/project'})).toThrow('invalid');dispose()});it('clears repository authority before switching from project A to project B',()=>{setAuthorizedWorkspace({handle:'workspace.project-a'});clearAuthorizedWorkspaceForProjectTransition('project-a','project-a');expect(getAuthorizedWorkspace()?.handle).toBe('workspace.project-a');clearAuthorizedWorkspaceForProjectTransition('project-a','project-b');expect(getAuthorizedWorkspace()).toBeUndefined()})})
