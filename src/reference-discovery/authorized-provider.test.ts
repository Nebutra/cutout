import{describe,expect,it,vi}from'vitest';import{createAuthorizedReferenceProvider}from'./authorized-provider';
const row={id:'r',title:'Ref',sourceUrl:'https://example.com/ref',contentSha256:'a'.repeat(64),license:{kind:'unknown',rationale:'Review required'},provenance:{provider:'mox-web',capturedAt:'2026-07-12T00:00:00.000Z',query:'brand'}};
describe('authorized reference provider',()=>{it('injects a host and preserves license/provenance instead of scraping',async()=>{const search=vi.fn(async()=>[row]),provider=createAuthorizedReferenceProvider({available:true,search});expect(await provider.search('brand')).toEqual([row]);expect(search).toHaveBeenCalledWith({query:'brand',limit:24})});it('rejects unavailable, invalid and over-limit hosts',async()=>{await expect(createAuthorizedReferenceProvider({available:false,search:vi.fn()}).search('brand')).rejects.toThrow('unavailable');await expect(createAuthorizedReferenceProvider({available:true,search:async()=>[row,row]},1).search('brand')).rejects.toThrow('limit')})})
