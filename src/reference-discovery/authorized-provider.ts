import{discoveredReferenceSchema,type ReferenceDiscoveryProvider}from'./runtime';
export interface AuthorizedDiscoveryHost{readonly available:boolean;search(input:{readonly query:string;readonly limit:number}):Promise<readonly unknown[]>}
export function createAuthorizedReferenceProvider(host:AuthorizedDiscoveryHost,limit=24):ReferenceDiscoveryProvider{return{available:host.available,async search(query){const value=query.trim();if(!host.available)throw new Error('Authorized reference discovery host is unavailable.');if(value.length<2||value.length>500)throw new Error('Reference query length is invalid.');const rows=await host.search({query:value,limit});if(rows.length>limit)throw new Error('Reference discovery host exceeded its result limit.');return rows.map((row)=>discoveredReferenceSchema.parse(row))}}}
