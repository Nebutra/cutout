import{useState}from'react';import{Layers,RefreshCw,Unplug}from'lucide-react';import{Button}from'@/components/ui/button';import{connectorCatalog}from'@/components/integrations/connector-catalog';import{useIntegrationConnections}from'@/components/integrations/IntegrationConnectionContext';import{IntegrationIcon}from'../integration-icons'
const oauthProviders=new Set(['cutout.github','cutout.notion'])
export function IntegrationsSection(){const controller=useIntegrationConnections(),[,render]=useState(0);const update=()=>render((v)=>v+1);return <section aria-labelledby="settings-integrations-heading"><div className="flex items-center gap-2.5"><Layers className="size-4 text-muted-foreground"/><h2 id="settings-integrations-heading" className="text-sm font-semibold">Integrations</h2></div><p className="mt-2 text-sm text-muted-foreground">Credentials remain in the desktop host. Cutout stores only opaque, revocable session handles.</p><div className="mt-4 divide-y divide-border rounded-md border border-border">{connectorCatalog.map((connector)=>{const state=controller.state(connector.id,connector.provider.id),oauth=oauthProviders.has(connector.id),busy=['authorizing','refreshing','revoking'].includes(state.status);return <div key={connector.id} className="flex items-center gap-3 px-3 py-3"><IntegrationIcon id={connector.id} name={connector.product.name}/><div className="min-w-0 flex-1"><p className="text-sm font-medium">{connector.product.name}</p><p className="text-xs text-muted-foreground">{label(state.status,connector.unavailableReason)}</p>{state.error?<p className="mt-1 text-xs text-destructive">{state.error}</p>:null}</div>{state.status==='connected'?<><Button size="icon-sm" variant="ghost" title="Refresh session" disabled={busy} onClick={()=>void controller.refresh(connector.id).then(update)}><RefreshCw className="size-4"/></Button><Button size="icon-sm" variant="ghost" title="Disconnect" disabled={busy} onClick={()=>void controller.disconnect(connector.id).then(update)}><Unplug className="size-4"/></Button></>:<Button size="sm" variant="outline" disabled={!oauth||state.status==='disabled'||busy} onClick={()=>void controller.begin(connector.id,connector.provider.id,scopes(connector.id)).then(update)}>{state.status==='authorizing'?'Waiting for callback':'Connect'}</Button>}</div>})}</div><p className="mt-3 text-xs text-muted-foreground">Plugin, MCP, CLI and local-vault integrations become available only after their host handshake is verified.</p></section>}
function label(status:string,reason?:string){if(status==='disabled')return reason??'Desktop provider is not installed.';if(status==='connected')return'Connected with a host-owned session.';if(status==='expired')return'Session expired. Refresh or reconnect.';if(status==='authorizing')return'Complete authorization in the provider window.';if(status==='refreshing')return'Refreshing session...';if(status==='revoking')return'Revoking session...';if(status==='error')return'Connection failed.';return reason??'Not connected.'}
function scopes(id:string){return id==='cutout.github'?['contents:read','pull_requests:write']:id==='cutout.notion'?['read_content','insert_content']:[]}
