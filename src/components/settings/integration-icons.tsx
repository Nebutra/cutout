import{FileText,FolderGit2,Pencil}from'lucide-react'
import{integrationIconRegistry}from'./integration-icon-registry'
export function IntegrationIcon({id,name}:{readonly id:string;readonly name:string}){const brand=integrationIconRegistry[id as keyof typeof integrationIconRegistry];if(brand?.svg)return <span role="img" aria-label={`${name} logo`} data-integration-icon={id} data-icon-source={brand.source} className="inline-block size-4 text-muted-foreground [&>svg]:size-full" dangerouslySetInnerHTML={{__html:brand.svg.replace('<svg','<svg aria-hidden="true" focusable="false"')}}/>;const Icon=id==='cutout.repository'?FolderGit2:id==='cutout.pencil'?Pencil:FileText;return <Icon role="img" aria-label={`${name} integration`} data-integration-icon={id} data-icon-source="Cutout generic" className="size-4 text-muted-foreground"/>}
