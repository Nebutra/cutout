import{describe,expect,it}from'vitest';import{SPEECH_MODEL_DIMENSIONS}from'./speech-model-dimensions';import{MODEL_DIMENSIONS}from'./model-dimensions';describe('settings routing domains',()=>{it('keeps six AI dimensions separate from two speech dimensions',()=>{expect(MODEL_DIMENSIONS.map(item=>item.task)).toEqual(['text','vision','webdev','image-to-webdev','image-generation','image-edit']);expect(SPEECH_MODEL_DIMENSIONS.map(item=>item.task)).toEqual(['asr','tts'])})})
