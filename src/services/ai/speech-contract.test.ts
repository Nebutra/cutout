import{describe,expect,it}from'vitest';import{CapabilityRequiredSpeechAdapter,speechReceiptSchema,speechRequestSchema,speechResponseSchema}from'./speech-contract';const hash='a'.repeat(64),at='2026-07-12T00:00:00.000Z'
describe('speech contracts',()=>{it('validates strict bounded request response and receipt',()=>{expect(speechRequestSchema.parse({protocol:'cutout.speech-request.v1',requestId:'r',capability:'asr',providerId:'p',model:'m',audio:{mediaType:'audio/wav',sha256:hash,bytes:new Uint8Array([1])}})).toBeTruthy();expect(speechResponseSchema.parse({protocol:'cutout.speech-response.v1',requestId:'r',capability:'asr',text:'hello',segments:[]})).toBeTruthy();expect(speechReceiptSchema.parse({protocol:'cutout.speech-receipt.v1',requestId:'r',capability:'asr',providerId:'p',model:'m',status:'succeeded',inputSha256:hash,outputSha256:hash,startedAt:at,completedAt:at})).toBeTruthy()});it('advertises no online speech and returns capability-required',async()=>{const adapter=new CapabilityRequiredSpeechAdapter();expect(adapter.capabilities).toEqual([]);await expect(adapter.execute(speechRequestSchema.parse({protocol:'cutout.speech-request.v1',requestId:'r',capability:'tts',providerId:'p',model:'m',text:'hello',voice:'v',outputMediaType:'audio/mpeg'}))).resolves.toEqual({ok:false,error:expect.stringContaining('capability-required')})});it('rejects empty audio',()=>{expect(()=>speechRequestSchema.parse({protocol:'cutout.speech-request.v1',requestId:'r',capability:'asr',providerId:'p',model:'m',audio:{mediaType:'audio/wav',sha256:hash,bytes:new Uint8Array()}})).toThrow()})})
