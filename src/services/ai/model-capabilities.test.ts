import{describe,expect,it}from'vitest';import{descriptorSupports,migrateLegacyAssignments,modelDescriptorSchema,modelTaskProfile}from'./model-capabilities'
describe('model capability domain',()=>{it('models atomic and composite task requirements without inventing a webdev model capability',()=>{expect(modelTaskProfile('webdev')).toMatchObject({required:['text','tools'],composite:true});expect(modelTaskProfile('image-to-webdev')).toMatchObject({required:['text','vision','tools'],composite:true});const descriptor=modelDescriptorSchema.parse({providerId:'p',model:'m',capabilities:['text','tools'],source:'provider'});expect(descriptorSupports(descriptor,modelTaskProfile('webdev'))).toBe(true);expect(descriptorSupports(descriptor,modelTaskProfile('image-to-webdev'))).toBe(false)});it('migrates old slots conservatively',()=>{const chat={providerId:'p',model:'chat'},image={providerId:'p',model:'image'},migrated=migrateLegacyAssignments({chat,image});expect(migrated.bindings).toEqual({text:chat,'image-generation':image});expect(migrated.bindings).not.toHaveProperty('vision');expect(migrated.bindings).not.toHaveProperty('image-edit')})})
