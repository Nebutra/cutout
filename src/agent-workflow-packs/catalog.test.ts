import{describe,expect,it}from'vitest';import{WorkflowPackCatalog}from'./catalog'
const pack=(version:string)=>({protocol:'cutout.workflow-pack.v1',id:'brand.launch',version,title:'Brand launch',description:'Generate and validate kit.',cutoutRange:'^0.1.0',capabilities:['export.brand-kit'],skillRefs:[],steps:[{id:'export',operation:'export.brand-kit',dependsOn:[],effect:'managed-export',approval:'explicit'}],evalCard:{datasetId:'brand.dataset',metrics:[{id:'schema',threshold:.9,weight:1}],minimumScore:.9},provenance:{producer:'cutout',capturedAt:'2026-07-12T00:00:00Z',contentSha256:(version==='1.0.0'?'a':'b').repeat(64)}})
describe('WorkflowPackCatalog',()=>{it('installs, upgrades and evaluates only compatible packs',()=>{const catalog=new WorkflowPackCatalog([pack('1.0.0'),pack('1.1.0')],{cutoutVersion:'0.1.0',capabilities:['export.brand-kit']},()=> '2026-07-12T00:00:00Z');expect(catalog.install('brand.launch','1.0.0').version).toBe('1.0.0');expect(catalog.upgrade('brand.launch').version).toBe('1.1.0');expect(catalog.evaluate('brand.launch',{schema:{score:1,evidenceIds:['schema.receipt']}}).status).toBe('passed');expect(catalog.list()[1]?.installed).toMatchObject({version:'1.1.0',evalStatus:'passed'})})})
