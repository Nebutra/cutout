import{describe,expect,it}from'vitest';import type{GlobalLibraryItem}from'@/global-library';import{curationEligibility,governedLibrarySearch}from'./search'
const item={id:'team.button',name:'Primary Button',description:'Accessible action',tags:['action'],updatedAt:'2026-07-12T00:00:00Z',origin:{kind:'generated',producer:'cutout'},license:{kind:'spdx',identifier:'MIT'},qualityReceipts:[{gate:'accessibility',status:'passed'}]}as unknown as GlobalLibraryItem
describe('library governance search',()=>{it('ranks deterministic metadata without pretending vector search',()=>{expect(governedLibrarySearch([item],{text:'button',namespace:'team',author:'cutout',licenses:['MIT'],qualityGates:['accessibility']})[0]?.item).toBe(item)});it('blocks curation on license or quality evidence',()=>{expect(curationEligibility(item,{allowedLicenses:['MIT'],requiredGates:['accessibility']})).toEqual({eligible:true,blockers:[]});expect(curationEligibility(item,{allowedLicenses:['Apache-2.0'],requiredGates:['build']})).toEqual({eligible:false,blockers:['license','quality:build']})})})
