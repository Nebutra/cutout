import { readdir, readFile } from 'node:fs/promises'
import { resolve } from 'node:path'
export async function workflowList(root){const packs=await all(root);return packs.map(({id,version,title,description,cutoutRange,capabilities,evalCard})=>({id,version,title,description,cutoutRange,capabilities,evalCard}))}
export async function workflowGet(root,id,version){const selected=(await all(root)).filter(pack=>pack.id===id&&(!version||pack.version===version)).sort((a,b)=>b.version.localeCompare(a.version))[0];if(!selected)throw new Error(`Workflow pack not found: ${id}${version?`@${version}`:''}.`);return selected}
export function workflowCompatibility(pack,input){const required=Number(String(pack.cutoutRange).match(/\d+/)?.[0]),actual=Number(String(input.cutoutVersion).split('.')[0]),missing=pack.capabilities.filter(cap=>!input.capabilities.includes(cap));return{compatible:required===actual&&!missing.length,missingCapabilities:missing,reason:required!==actual?'Cutout major version is outside the pack range.':missing.length?'Required capabilities are unavailable.':undefined}}
async function all(root){const dir=resolve(root,'.cutout','workflows'),names=await readdir(dir).catch(error=>error?.code==='ENOENT'?[]:Promise.reject(error)),packs=[];for(const name of names.filter(name=>name.endsWith('.json')).sort()){const pack=JSON.parse(await readFile(resolve(dir,name),'utf8'));if(pack?.protocol!=='cutout.workflow-pack.v1'||typeof pack.id!=='string'||!Array.isArray(pack.capabilities)||!Array.isArray(pack.steps))throw new Error(`Invalid Cutout workflow pack: ${name}.`);packs.push(pack)}return packs}
